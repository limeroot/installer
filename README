This gnu/linux distribution had as intent be a flexible base where the sysadmin is who decide the type of server want build.
The GNU/Linux LimeRoot base is ampliable with many features called "modules" and by default have a friendly web administration page.
